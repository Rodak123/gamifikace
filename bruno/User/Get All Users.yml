info:
  name: Get All Users
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_PATH}}/user/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
