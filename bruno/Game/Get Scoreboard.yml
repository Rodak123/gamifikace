info:
  name: Get Scoreboard
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_PATH}}/game/scoreboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
