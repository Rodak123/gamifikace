info:
  name: "Dev Login "
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_PATH}}/auth/loginDev"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
