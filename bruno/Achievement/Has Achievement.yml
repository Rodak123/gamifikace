info:
  name: Has Achievement
  type: http
  seq: 7

http:
  method: POST
  url: "{{API_PATH}}/achievements/hasAchievement"
  body:
    type: json
    data: |-
      {
        "user": { "id": "" },
        "achievement": {"key": "react-dev"}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
