info:
  name: Get Mine Achievements
  type: http
  seq: 6

http:
  method: POST
  url: "{{API_PATH}}/achievement/mine"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
