info:
  name: Get All Achievements
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_PATH}}/achievement/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
